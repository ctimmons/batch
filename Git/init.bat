@echo off

git config --global user.name "ctimmons"
git config --global user.email "chris@crtimmonsinc.com"

git config --global color.status.updated magenta bold
git config --global color.status.untracked magenta bold
git config --global color.status.changed yellow bold

git config --global color.diff.old magenta bold

git config --global push.default simple

